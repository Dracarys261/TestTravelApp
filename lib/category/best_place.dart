import 'package:flutter/material.dart';

class BestPlacePage extends StatelessWidget {
  const BestPlacePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title : Text('Bestplace')
      ),
    );
  }
}
